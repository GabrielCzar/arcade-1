#include <iostream>
using namespace std;

int main(){
    int a;
    cin >> a;
    if(a > 0)
        cout << "positivo";
    if(a < 0)
        cout << "negativo";
    else
        cout << "nulo";
}
